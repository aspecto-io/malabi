{
    "name": "example-runner",
    "version": "0.0.1-alpha.1",
    "license": "Apache-2.0",
    "private": true,
    "scripts": {
        "start": "npm-run-all --parallel service-under-test downstream-service test-runner",
        "service-under-test":"cd ./service-under-test && yarn start",
        "test-runner":"cd ./tests-runner && sleep 8 && yarn test:example",
        "downstream-service":"cd ./downstream-service && yarn start"
    },
    "devDependencies": {
        "npm-run-all": "^4.1.5"
    }
}
